export const PROFILE_FIELDS = `
  id,
  username,
  avatar_url,
  locale,
  created_at
`

export const PROFILE_ID_FIELDS = 'id'

export const TEMPLATE_FIELDS = `
  id,
  name,
  image_url,
  category,
  created_at
`

export const MEME_FIELDS = `
  id,
  user_id,
  template_id,
  title,
  image_url,
  language,
  view_count,
  created_at
`
